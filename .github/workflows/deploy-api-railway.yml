name: Deploy API to Railway

on:
  push:
    branches:
      - main
    paths:
      - "package.json"
      - "package-lock.json"
      - "Dockerfile"
      - "railway.json"
      - "apps/api/**"
      - ".github/workflows/deploy-api-railway.yml"
  workflow_dispatch:

concurrency:
  group: railway-api-production
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy API
    runs-on: ubuntu-latest
    env:
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      RAILWAY_PROJECT_ID: 72c29bb4-f05e-4b2f-9d03-87d25b5114d7
      RAILWAY_ENVIRONMENT: production
      RAILWAY_SERVICE: api
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy API to Railway
        shell: bash
        run: |
          if [ -z "$RAILWAY_TOKEN" ]; then
            echo "GitHub secret RAILWAY_TOKEN is missing."
            echo "Create it in https://github.com/AgpSistemas/condo-access-clean/settings/secrets/actions to enable automatic deploys."
            exit 0
          fi

          railway up \
            --project "$RAILWAY_PROJECT_ID" \
            --environment "$RAILWAY_ENVIRONMENT" \
            --service "$RAILWAY_SERVICE" \
            --ci \
            --message "GitHub auto deploy ${GITHUB_SHA}"
